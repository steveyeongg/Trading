[project]
name = "explanation-engine"
version = "0.1.0"
description = "LLM-generated trade rationale. DeepSeek (OpenAI-compatible) with server-side prompt cache; templated fallback."
requires-python = ">=3.12"
dependencies = [
    "atlas-shared",
    "openai>=1.40",
]

[tool.uv.sources]
atlas-shared = { workspace = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/explanation_engine"]
